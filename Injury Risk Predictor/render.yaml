# Render Blueprint - deploys both API and frontend
# https://render.com/docs/blueprint-spec

services:
  # FastAPI Backend
  - type: web
    name: epl-injury-api
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r api/requirements.txt && pip install pandas numpy scikit-learn catboost lightgbm xgboost joblib beautifulsoup4 requests
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /api/health

  # Next.js Frontend
  - type: web
    name: epl-injury-frontend
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm run start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NEXT_PUBLIC_API_URL
        fromService:
          name: epl-injury-api
          type: web
          property: host
